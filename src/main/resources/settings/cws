import clustersettings
import textindex
import cwin
repository.dir = /user/jeroenv/output/cws
repository.defaultentityreader = EntityReaderCW
#alternatively you can assign different entity readers based on file extensions
repository.assignentityreader  = EntityReaderCW .gz
repository.prefix = cws
repository.partitions = 720
#index.onlypartition = 269

repository.collectionid = DocLiteral:collectionid
+repository.feature = TermInverted:all
+repository.feature = TermInverted:title
+repository.feature = TermDF
+repository.feature = DocLiteral:literaltitle
+repository.feature = DocLiteral:url
+repository.feature = DocForward:title
+repository.feature = DocForward:all
+repository.feature = DocTF:all
+repository.feature = TermTF

repository.spamlist=input/clueweb
repository.spamthreshold=70
vocabulary.mintf = 5

import cwtokenizer

#mapred.max.split.size = 4000000
mapred.task.timeout = 3600000
 
