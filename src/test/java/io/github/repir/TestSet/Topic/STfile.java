package io.github.repir.TestSet.Topic;
import io.github.repir.tools.io.Datafile;
import io.github.repir.tools.lib.Log;

/**
 *
 * @author Jeroen Vuurens
 */
public class STfile {
  public static Log log = new Log( STfile.class ); 
  
   public static void main(String[] args) {
//      Datafile df = new Datafile("/home/jer/Desktop/st11.topics.txt");
//      log.info("length %d", df.getLength());
//      TopicReaderSessionTrack f = new TopicReaderSessionTrack(df);
//      log.info("aap");
//      f.openRead();
//      while (f.next()) {
//        log.info("%d %s %s", f.sessionnum.get(), f.title.get(), f.sessionstarttime.get());
//     }
  }
}
